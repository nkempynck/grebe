node_modules
dist
*.log
.DS_Store
.vite
.env
.env.local
.env.*.local
# env template kept out too — the backend config isn't part of the public repo
.env.example

# compiled config / build metadata (source is the .ts)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# stray backups
*.bak

# backend setup script — run manually in Supabase, kept out of the repo
supabase/

# personal dev notes / roadmap — not part of the package
dev-notes.md

# backend setup docs — kept local, out of the repo (see also supabase/)
SUPABASE_SETUP.md

# build artifact — regenerated each taxonomy build
scripts/name-review.tsv

# local data backups
taxonomy.backup.json
src/data/taxonomy.json.bak
taxon_index.backup.*.json
